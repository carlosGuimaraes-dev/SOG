version: "3.8"

# Ambiente de desenvolvimento local
# Uso: docker-compose -f docker-compose.dev.yml up

services:
  agente:
    build:
      context: .
      dockerfile: agente/Dockerfile
    container_name: custas-agente-dev
    env_file: .env.agente
    environment:
      STORAGE_STATE_DIR: /dados/auth
    volumes:
      - ./dados:/dados
      - ./agente/src:/app/src:ro
      - ./shared:/app/shared:ro
    restart: unless-stopped

  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    container_name: custas-api-dev
    env_file: .env.api
    volumes:
      - ./dados:/dados
      - ./api/src:/app/src:ro
      - ./shared:/app/shared:ro
    command: uvicorn src.app:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: custas-frontend-dev
    working_dir: /app
    volumes:
      - ./frontend/src:/app/src:ro
      - ./frontend/index.html:/app/index.html:ro
      - ./frontend/vite.config.ts:/app/vite.config.ts:ro
      - ./frontend/tsconfig.json:/app/tsconfig.json:ro
      - ./frontend/tsconfig.node.json:/app/tsconfig.node.json:ro
    ports:
      - "3001:3000"
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    container_name: custas-nginx-dev
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx-dev.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - api
      - frontend
    restart: unless-stopped

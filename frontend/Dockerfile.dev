FROM node:20-alpine

WORKDIR /app

# Instala dependências primeiro (cache de layer)
COPY package.json package-lock.json ./
RUN npm install

# O código fonte será montado via volume em desenvolvimento
EXPOSE 3000

CMD ["npm", "run", "dev", "--", "--host"]

# Role prompts for Codex

These files are the original Kimi system prompts.

Notes:

- Keep them stable unless the underlying role behavior must change.
- They may contain Kimi-specific placeholders such as `KIMI_WORK_DIR` and `KIMI_NOW`.
- When using them in Codex, translate those placeholders to the current repository and session context.

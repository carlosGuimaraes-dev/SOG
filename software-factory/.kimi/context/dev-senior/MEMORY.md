# MEMORY — Dev Senior

> Arquivo dinâmico. Registre aprendizados técnicos, padrões locais
> identificados e débitos técnicos encontrados durante a implementação.

---

## Padrões de código identificados no projeto

<!-- Convenções reais encontradas no codebase — não as ideais, as reais.
Exemplo:
- Todos os handlers async usam `async def` com `await` explícito
- Erros retornam dict `{"error": "mensagem"}` com status HTTP adequado
- Imports sempre absolutos a partir da raiz do projeto
-->

_Nenhum padrão registrado ainda._

---

## Pegadinhas e gotchas do projeto

<!-- Problemas não óbvios encontrados durante implementações anteriores.
Exemplo:
- O ORM não suporta upsert nativo — usar merge() do SQLAlchemy
- O middleware de autenticação não roda em rotas prefixadas com /public/
- Testes que envolvem banco precisam de fixture `db_session` do conftest.py
-->

_Nenhum gotcha registrado ainda._

---

## Débitos técnicos identificados (fora do escopo atual)

<!-- Problemas que vi mas não corrigi. O CEO precisa saber.
Exemplo:
- `utils/validators.py` linha 23: regex de email falha com TLDs longos
- `models/user.py`: campo `phone` sem validação de formato
-->

_Nenhum débito registrado ainda._

---

## Histórico de implementações

<!-- Referência do que foi feito para contexto futuro.
Exemplo:
- 2024-01-20: Módulo auth/jwt.py — encode/decode + endpoints /auth/token
- 2024-01-22: CRUD completo de users em routers/users.py
-->

_Nenhuma implementação registrada ainda._
